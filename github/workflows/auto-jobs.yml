name: Auto Sarkari Jobs

on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: Start Auto Sync
        run: |
          echo "Checking latest jobs every 5 minutes..."
