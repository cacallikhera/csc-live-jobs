name: Sarkari Live Auto Update

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - name: Auto Live Jobs
        run: echo "Every 5 minutes running..."
